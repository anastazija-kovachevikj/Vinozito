<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/circlegrey" /> <!-- Background color -->
    <corners android:radius="8dp" /> <!-- Corner radius -->
    <stroke
        android:width="0dp"
        android:color="@color/darkgrey2" /> <!-- border color and width -->
</shape>