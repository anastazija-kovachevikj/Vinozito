<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/blue_scroll"/>
            <corners android:topLeftRadius="10dp" android:topRightRadius="10dp"/> <!-- Rounded left corners -->
        </shape>
    </item>

    <!-- Border with adjusted position to show the stroke -->
    <item android:left="0dp" android:right="-2dp" android:top="0dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <stroke
                android:width="2.5dp"
                android:color="@color/babyblue" />
            <corners android:topLeftRadius="10dp" android:topRightRadius="10dp" /> <!-- Apply rounded corners to border -->
        </shape>
    </item>

</layer-list>
