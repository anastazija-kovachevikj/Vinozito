<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/login_blue" /> <!-- Background color -->
    <corners android:radius="13dp" /> <!-- Corner radius -->
    <stroke
        android:width="0dp"
        android:color="@color/login_blue" /> <!-- border color and width -->
</shape>