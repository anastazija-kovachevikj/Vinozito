<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Vinozhito" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/btncolor</item>
        <item name="android:textColor">@color/darkgrey2</item>
         <item name="android:windowFullscreen">true</item>

<!--        <item name="android:windowBackground">@drawable/scrolling_background</item>-->
    </style>

    <style name="Theme.Vinozhito" parent="Base.Theme.Vinozhito" />
</resources>
